<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.thangiee.LoLWithFriends"
          android:versionCode="1"
          android:versionName="1.0">

    <uses-sdk android:minSdkVersion="14"
              android:targetSdkVersion="19"/>

    <uses-permission android:name="android.permission.INTERNET"/>

    <application android:label="@string/app_name"
                 android:theme="@android:style/Theme.Holo.Light.DarkActionBar"
                 android:allowBackup="true"
                 android:name="com.activeandroid.app.Application">
        <meta-data android:name="AA_DB_NAME" android:value="MessageLogs.db"/>

        <service android:name=".services.FriendListService" />
        <service android:name=".services.ChatService" />

        <activity android:name=".activities.ServerSelection"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".activities.LoginActivity"
                  android:label="LogIn">
            <meta-data
                    android:name="android.support.PARENT_ACTIVITY"
                    android:value=".activities.ServerSelection"/>
        </activity>

        <activity android:name=".activities.MainActivity"
                  android:label="@string/app_name"/>
    </application>
</manifest>
